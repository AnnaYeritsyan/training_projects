import { Box, ListItem, ListItemButton,ListItemText } from '@mui/material';
import MovieAdminButton from '../MovieHeader/MovieAdminButton';

        
const MovieAdmin = () => {
    return (
        <Box>
<MovieAdminButton/>

        </Box>
    );
};
export default MovieAdmin;